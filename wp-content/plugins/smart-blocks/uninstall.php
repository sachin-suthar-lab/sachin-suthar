<?php
/**
 * Smart Blocks uninstall.
 *
 * The plugin stores no options, post meta, custom tables, or scheduled events,
 * so uninstall is a no-op. This file exists so WordPress doesn't trigger the
 * legacy deactivation hook fallback.
 *
 * @package SmartBlocks
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
